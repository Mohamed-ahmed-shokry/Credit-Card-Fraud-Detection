## Problem

<!-- What is broken, missing, or risky today? -->

## Approach

<!-- What does this change do, and why this approach over alternatives? -->

## Verification

<!-- Commands you ran locally, e.g.:
python -m ruff format --check src tests
python -m ruff check src tests
python -m mypy src
python -m pytest --cov=fraud_detection --cov-report=term-missing
-->

## Operational / model-risk impact

<!-- Does this change training behavior, metrics, the API contract, the artifact
format, or anything a deployed model or caller depends on? -->

## Migration

<!-- Does this require retraining, a config change, or anything else for
existing artifacts or deployments? Write "None" if not applicable. -->
